---
const menuitems = [
  {
    title: "About",
    path: "#about",
  },
  {
    title: "Projects",
    path: "#projects",
  },
  {
    title: "Contact",
    path: "#contact",
  },
];
---

<header>
  <nav class="bg-background border-y-accent mt-10 border-b-2">
    <div class="flex items-center justify-center p-4 mx-auto">
      <ul class="flex flex-row gap-10">
        {
          menuitems.map((item) => (
            <li>
              <a
                aria-label="navigation link"
                href={item.path}
                class="font-body text-text hover:text-accent2 lg:px-3 flex py-1 font-bold uppercase"
              >
                {item.title}
              </a>
            </li>
          ))
        }
      </ul>
    </div>
  </nav>
</header>
